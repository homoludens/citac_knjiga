*.wav
# Golden reference vectors are tracked (LFS) — generated audio is not.
!model-tools/reference/*.wav
.agents/
.hermes/
# Python virtualenvs (uv)
.venv/
model-tools/.venv/
__pycache__/
# Regenerable ONNX export candidate (large; identity tracked via
# model-tools/export/manifest.json sha256, not committed)
model-tools/export/*.onnx
# Optional local desktop-to-Android parity bundle (large generated WAV files).
model-tools/parity/desktop-onnx-vectors/
# Android/Gradle build outputs are local and reproducible.
**/build/
.gradle/
